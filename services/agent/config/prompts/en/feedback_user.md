# Runtime Evidence

Below is the sentence-level analysis evidence for the current SpeakSure++ run.

Follow the schema exactly, do not omit any segment, and do not output any text outside the JSON object.

{payload_json}
