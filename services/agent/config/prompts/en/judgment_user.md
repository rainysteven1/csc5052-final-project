# Runtime Evidence

Below is the structured analysis evidence for the current SpeakSure++ run, together with the rule-based baseline scoring result.

Produce the intermediate judgment first and return JSON only.

{payload_json}
